<div class ='container'>

<div class = 'page-header'>
<h1>Select a Month</h1>
</div>

<ul>
<% @months.each do |month| %>
  <p>
    <li><%= link_to(month.name, month_countries_path(month), class: 'btn btn-info btn-lg', role: 'button') %></li>
  </p>
<% end %>
</ul>

</div>
