<h1>Countries#index</h1>
<p>Find me in app/views/countries/index.html.erb</p>

<ul>
<% @countries.each do |country| %>
  <li><%= link_to(country.name, country_cities_path(country)) %></li>
<% end %>
</ul>
